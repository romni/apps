apiVersion: fleet/v1
name: plugins
# plugins is fleet tooling (the dev@homelab marketplace), so it keeps its
# homelab-org home permanently; the explicit repo: override below sets that.
repo: homelab/plugins
namespace: apps # grandfathered into the shared namespace (pre-dates per-app namespaces)
pages: [plugins]
